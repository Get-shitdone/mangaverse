# Optional — only needed for full Comics catalog. App degrades gracefully without it.
COMICVINE_API_KEY=

# Optional — Google Books API key for higher rate limits on novel previews.
GOOGLE_BOOKS_API_KEY=
